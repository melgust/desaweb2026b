spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/catalog}
  # jpa:
  #   show-sql: true
  #   properties:
  #     hibernate:
  #       format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.springframework.web: DEBUG
